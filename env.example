PORT=3000
NODE_ENV=development

#DB Connection
DB_CONNECTION=

DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD= 
DB_SSL=

# Supabase (Storage + Database)
SUPABASE_URL=
# Use the service role key on the server only (NEVER expose publicly)
SUPABASE_SERVICE_ROLE_KEY=
# Optional: if you ever use supabase-js in the browser for non-admin calls
SUPABASE_ANON_KEY=
# Bucket name where assets are stored
SUPABASE_BUCKET=
